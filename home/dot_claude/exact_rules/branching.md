# Branching

Create branches from `main`, named `gc/<type>/<short-description>`:

- `gc/feat/user-auth`
- `gc/fix/login-redirect`
- `gc/chore/upgrade-deps`

Rebase on `main` before opening a pull request; delete branches after merge.
