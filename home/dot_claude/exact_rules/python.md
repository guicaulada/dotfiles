# Python

## Running Scripts

- Always use `uv run` to execute Python scripts — never use `python` or `python3` directly
- Use `uv run` for one-off script execution with inline dependencies
- Prefer `uv` for all Python tooling: package management, virtual environments, and script execution
